var auth = require('./lib/auth');
var container = require('./lib/container');
var agent = require('./lib/agent');
var util = require('./lib/util');

module.exports = {
    auth: auth,
    container: container,
    agent: agent,
    setAPIkey: util.setAPIkey,
    getAPIkey: util.getAPIkey
};
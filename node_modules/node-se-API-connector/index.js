var auth = require('./lib/auth');
var container = require('./lib/container');
var agent = require('./lib/agent');
var utils = require('./lib/utils');

module.exports = {
    auth: auth,
    container: container,
    agent: agent,
    setAPIkey: utils.setAPIkey,
    getAPIkey: utils.getAPIkey
};